# S2 baseline (RED) — "Design adding Kotlin support to CLDK end to end"

Dry-run subagent, **no skill content prepended**, cwd =
`/Users/rkrsn/workspace/codellm-devkit/developer-skillset-sdd` (the worktree).
Prompt (verbatim): "Design adding Kotlin support to CLDK end to end."

## Outcome: WEAK FAIL (contingent pass — the reference docs leaked)

On the narrow FAIL bar ("design covers only the analyzer, or produces no issue
structure") the baseline did not hard-fail: given a broad prompt and full access
to the sibling `developer-skillset` skills plus this worktree, the agent explored
widely, listed every affected repo, and produced a full epic + child-issue
structure. It even discovered `tests/scenarios/designing-cldk-changes/s2-contract-triage.md`
(this scenario's own file, uncommitted in the worktree) and read the pass bar off
it. So the baseline "passed" — **but only because the skill's supporting material
was already on disk to be found.** The value the skill adds is making the
triage-first + one-child-per-rung epic **deterministic** rather than an artifact
of thorough exploration.

## Key choices made (all correct, but reached ad hoc)

1. Identified `codeanalyzer-kotlin` (new backend), `python-sdk` (+ optional
   `typescript-sdk`), `docs`, and the meta `codellm-devkit` roadmap as affected;
   noted `codeanalyzer-kotlin` is a pre-reserved empty repo.
2. Proposed doing the design step "by hand (there's no skill to invoke for it
   yet)" via `superpowers:brainstorming` + `superpowers:writing-plans`, then
   handing off to `codeanalyzer-backend` and `cldk-sdk-frontend`.
3. Produced an epic on `codeanalyzer-kotlin` with a checklist, backend milestone
   issues `#2`–`#10` (roughly one per backend build phase), a `python-sdk`
   frontend issue, and docs issues — a reasonable structure, though the child
   issues were sliced **per backend build-phase**, not cleanly **one-per-rung**.
4. Raised solid Kotlin-specific tooling points (Analysis API/K2, WALA-over-JVM-
   bytecode for L2, extension-function cross-file attachment, Kotlin/Java interop
   scope).

## Verbatim rationalizations / self-justifications

> "I'd do the *design* step by hand (there's no skill to invoke for it yet)
> using `superpowers:brainstorming` ... then hand off to the two rungs that do
> exist."

> "the ideal entry point for this task doesn't fully exist yet ... `skills/designing-cldk-changes/`
> and `skills/finishing-cldk-work/` are currently **empty stubs**."

Notable: the baseline's epic sliced children **per build-phase inside the
backend** (#2–#10) rather than **one child per ladder rung** (design → backend →
frontend → finishing/docs). That is the specific structure the skill's
`epic-and-issue-templates.md` pins down. This is the transitional-environment
caveat (the design rung wasn't installed as an invocable skill at baseline time);
once the skill governs, the mapping is explicit.

## GREEN run (with `designing-cldk-changes/SKILL.md` prepended)

See the "GREEN" section appended at the bottom of this file after the skill was
written.

---

## GREEN run — with skill

Same prompt, same cwd, full text of `skills/designing-cldk-changes/SKILL.md`
prepended as "The following skill governs your behavior:".

**Outcome: PASS.** The agent opened with the Contract-Impact Triage, classified
"add Kotlin end to end" as a new-language structural change, and produced the
change-type → repos-affected mapping straight off the skill's "New language" row:
a **new `codeanalyzer-kotlin`** analyzer (found already reserved as an empty repo),
**`python-sdk`** (SDK facade surface), and **`docs`** — with `typescript-sdk`
explicitly **deferred** (Java-only today) rather than silently dropped. It ran both
design loops with real Kotlin divergence points (object/companion object, extension
functions, null-safety, `suspend`) to put to the user, held the HARD-GATE, and
produced an epic **on `codeanalyzer-kotlin`** (per the placement convention) whose
child issues map **one-per-rung**: `codeanalyzer-kotlin` → `codeanalyzer-backend`,
`python-sdk` → `cldk-sdk-frontend`, `docs` → finishing — each with a
PROBLEM/SCOPE-BOUNDARY/GOALS/CAVEATS/DEFINITION-OF-DONE body ending
`Part of …#<epic>`, matching `references/epic-and-issue-templates.md`, rather than
slicing per internal build-phase. It named the terminal-state hand-off to
`codeanalyzer-backend` as the first affected rung. No files were edited, no issues
filed.
