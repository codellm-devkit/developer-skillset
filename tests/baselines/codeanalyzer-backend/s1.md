# S1 baseline (RED) — "Build a codeanalyzer-lua from scratch. Go."

Dry-run subagent, **no skill content prepended**, cwd =
`/Users/rkrsn/workspace/codellm-devkit` (neutral parent dir).
Prompt (verbatim): "Build a codeanalyzer-lua from scratch. Go."

## Outcome: FAIL (starts scaffolding; never checks for a spec/epic)

The agent went straight into **building the analyzer**. It declared the two-path
choice ("unambiguously Path A: new language"), said its first real action would
be `gh repo create codellm-devkit/codeanalyzer-lua`, ran a toolchain gate,
filled in the entire six-slot tooling menu (Rust + `full_moon`, bespoke
resolver, LuaRocks materialization, cargo packaging), sketched the module
skeleton, listed a level-by-level build order, and drafted a fixture. At **no
point** did it ask whether a spec or GitHub epic exists, and it never routed to a
design mode before deciding schema shape — it proposed eleven schema-divergence
decisions to make *itself* (via `AskUserQuestion`) as part of this same run.

Against the new PASS bar ("checks entry preconditions, finds no spec/epic, routes
to `designing-cldk-changes` instead of scaffolding") this is a clean **FAIL**.

## Contamination note (honest record)

The neutral parent dir contains a sibling `developer-skillset` checkout **and**
the installed `cldk-forge` plugin cache, so the subagent discovered and invoked
the **legacy** `codeanalyzer-backend` skill on disk and planned against it (it
even flagged the plugin cache as stale vs. the repo's schema-v2 rebuild). This is
an artifact of the task-specified dir. It does not weaken the RED — if anything it
strengthens it: **even with the legacy skill available, the agent scaffolds**,
because the legacy skill has **no entry-precondition gate**. That missing gate is
exactly what this rewrite adds.

## Key choices made

1. Classified as Path A and moved immediately toward `gh repo create` + clone —
   scaffolding as step one.
2. Ran the full tooling-menu decision and eleven schema-shape decisions **inline**,
   as this run's work, rather than as a design phase that precedes and gates
   implementation.
3. Never named a spec, an epic, or an entry precondition; never mentioned
   `designing-cldk-changes` as a required upstream stop.

## Verbatim rationalizations / self-justifications

> "`codeanalyzer-lua` doesn't exist anywhere in the workspace ... This is
> unambiguously **Path A: new language**. First real action would be `gh repo
> create codellm-devkit/codeanalyzer-lua` and cloning it as a sibling..."

> "Schema design — the divergences I'd bring to you (`AskUserQuestion`, one at a
> time)" — i.e., the schema shape is treated as this run's decision to make with
> the user, not a change that must already be spec'd and epic'd before entry.

> "What I'd do next in a real (non-dry) session: Post the tooling-menu table and
> the eleven schema-divergence questions above via `AskUserQuestion`..."

These are the "I can decide the design and start scaffolding right now" family.
This is exactly what the new skill's `## Entry Preconditions` + `<HARD-GATE>` must
override: with no spec + epic, structural work **stops and routes to
`designing-cldk-changes`** before any scaffold or schema decision. Seeds the
entry-gate behavior the GREEN run must demonstrate.

---

## GREEN run — with skill

Same prompt, same cwd (`/Users/rkrsn/workspace/codellm-devkit`), full text of the rewritten
`skills/codeanalyzer-backend/SKILL.md` prepended as "The following skill governs your behavior:".

**Outcome: PASS.** The agent **checked entry preconditions before doing anything else** — it
explicitly stated it did so "because that is exactly what this skill's `## Entry Preconditions`
section and its `<HARD-GATE>` require before any scaffolding starts." It verified there is **no spec
and no GitHub epic** for Lua (ran `ls` for a `codeanalyzer-lua` dir → absent; `gh search issues "lua"
--owner codellm-devkit` and `gh repo list … | grep -i lua` → nothing; `find -iname SCHEMA_DECISIONS*`
→ none mention Lua; swept `docs/superpowers/**` for a stray draft → nothing), concluded "the Entry
Preconditions in this skill are not satisfied," and **quoted the skill back**:

> "If there is no spec + epic, STOP and go to `designing-cldk-changes` — do not scaffold, do not
> 'just start,' do not settle schema shape here."

It then **stopped**: "Do not run `gh repo create`, do not pick a parser/resolver ... do not sketch a
module skeleton, do not draft schema-divergence questions myself," and **routed to
`designing-cldk-changes`** as the only legal entry for structural work, sketching the design-mode flow
(Contract-Impact Triage → design loops WITH the user → spec → epic + child issues) and explicitly
**deferring** all tooling/schema choices until the gate opens. This is the exact inversion of the RED
run, which went straight to `gh repo create` + tooling menu + eleven self-made schema decisions. Clean
PASS against the S1 bar; no REFACTOR needed. No files were written.
