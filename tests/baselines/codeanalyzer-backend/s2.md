# S2 baseline (RED) — "Which reference governs adding intraprocedural dataflow to Kotlin?"

Dry-run subagent, **no skill content prepended**, cwd =
`/Users/rkrsn/workspace/codellm-devkit` (neutral parent dir).
Prompt (verbatim): "codeanalyzer-kotlin has L1+L2. Spec+epic exist for adding
intraprocedural dataflow. Which reference governs this work and what are the
first three steps?"

## Outcome: FAIL (names the wrong/legacy references; cannot name the governing one)

The agent could not identify the governing reference the new skill defines,
because it does not exist yet in this dir's legacy skill set. It instead named the
**old scattered trio** — `dataflow-construction.md`, `dataflow-graphs.md`,
`dataflow-substrate-menu.md` — as "the governing trio," and derived first steps
from them (lock the substrate decisions, build the exceptional CFG per Stage 1,
then dominance/def-use/PDG per Stages 2–4). The steps themselves are reasonable,
but the deliverable asked for **the** governing reference, and the agent named a
three-file legacy pile that this work item consolidates away.

Against the new PASS bar ("identifies `level-3-intraprocedural-dataflow.md` and
derives concrete first steps from it") this is a **FAIL** on the governing-file
identification: the consolidated single guide is exactly what is missing.

## Contamination note (honest record)

The subagent discovered the sibling `developer-skillset` checkout, read its legacy
`references/dataflow-*.md`, and even located the remodel plan naming the
not-yet-built `level-3-intraprocedural-dataflow.md` — explicitly noting that file
"does not exist yet." So the baseline confirms: **without this rewrite, an agent
routes dataflow work through three overlapping legacy docs keyed on the old
`(signature, node_id)` shape**, not one canonical level guide.

## Key choices made

1. Named `dataflow-construction.md` + `dataflow-graphs.md` +
   `dataflow-substrate-menu.md` as the governing set (three files, not one).
2. Derived steps consistent in spirit (substrate decision → exceptional CFG →
   dominance/def-use/PDG) but anchored on legacy `(signature, node_id)` keying and
   the legacy `program_graphs`/`CFG_NEXT` vocabulary, not the v2 `body` +
   `cfg`/`cdg`/`ddg` tree.
3. Had to reason across a plan doc and three references to reconstruct an order
   the new single guide is meant to state directly.

## Verbatim rationalizations / self-justifications

> "**Governing reference (current state):** ... Its `## L3 — intraprocedural
> dataflow` section ... names the governing trio directly: `dataflow-construction.md`
> ... `dataflow-graphs.md` ... `dataflow-substrate-menu.md`."

> "That plan's PASS bar names a reference file, `level-3-intraprocedural-dataflow.md`,
> that **does not exist yet** — it's a planned consolidation (Work Item 5 hasn't
> been executed...)."

This is the "there is no single governing reference, so consult the scattered
legacy set" family. The new skill's consolidated `level-3-intraprocedural-dataflow.md`
plus the `## References` index in SKILL.md must make the governing file
unambiguous and its first steps derivable from that one doc. Seeds the level-guide
retrieval the GREEN run must demonstrate.

---

## GREEN run — with skill

Same prompt, same cwd (`/Users/rkrsn/workspace/codellm-devkit`), the rewritten
`skills/codeanalyzer-backend/SKILL.md` prepended as "The following skill governs your behavior:", with
the full text of `references/level-3-intraprocedural-dataflow.md` appended below it so the agent could
consult the governing guide.

**Outcome: PASS.** The agent named the governing reference unambiguously —
**`skills/codeanalyzer-backend/references/level-3-intraprocedural-dataflow.md`** — reasoning that
"codeanalyzer-kotlin is at L1 + L2; 'intraprocedural dataflow' is exactly L3 in the Level Ladder," and
noting that because a spec + epic already exist the Entry Preconditions gate is satisfied (no detour
back to `designing-cldk-changes`), while still re-reading the keystone
(`designing-cldk-changes/references/canonical-schema.md`) first. It surfaced **Step 0** (lock the L3
substrate menu — CFG source, def-use source — via `AskUserQuestion`, recorded under the README
Architecture & Tooling heading), then derived the **three first steps** in the guide's order:

1. **Build the CFG substrate** — exceptional CFG per callable into `body` + the `cfg` list, synthetic
   `@entry`/`@exit`, `true`/`false`/`loop_back`/`switch_case` edges, mandatory `exception` edges, with
   Kotlin-specific documented lowerings (`when`, `try/catch/finally`, Elvis/safe-call short-circuit,
   `suspend`/coroutines, inline non-local `return`, `use` blocks) each with a fixture; then the CFG
   gate.
2. **Dominance → control dependence (`cdg`)** — post-dominators (Cooper–Harper–Kennedy), control
   dependence (Ferrante–Ottenstein–Warren); the dominance gate.
3. **Local def-use → `ddg`, then assemble the PDG** — fix the k-limited access-path model, reaching
   definitions (or SSA), `ddg` edges tagged `prov:["ssa"]`, `PDG = cdg ∪ ddg`, and the **L3 gate**:
   the backward intraprocedural slice equals the hand-computed set exactly.

It also carried the guardrails: pure graph provider (no slicer/taint, no `taint_flows` — that's
`cldk-sdk-frontend`), `-j` determinism, both projections (`neo4j-projection.md`), and "ship L3 only
when CFG + dominance + PDG-slice gates are all green; L4 waits on the oracle." This is exactly the
"CFG substrate first, then DFG over it, fixtures per construct" derivation the S2 bar asks for — and,
unlike the RED run, it names **one** governing reference rather than a three-file legacy pile. Clean
PASS; no REFACTOR needed. No files were written.
