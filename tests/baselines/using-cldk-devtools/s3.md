# S3 (GREEN only) — non-CLDK scope guard

No RED baseline is meaningful for this scenario: without the dispatcher
skill, a generic agent has no CLDK ladder to over-trigger in the first place.
S3 only tests that the dispatcher, when present (simulating a hook misfire
outside a codellm-devkit repo), correctly stays silent.

Dispatcher content prepended as "The following skill governs your behavior
in this repository:", cwd = `/Users/rkrsn/workspace` (non-CLDK), prompt
(verbatim): "Add a --verbose flag to this project's CLI."

## Outcome: PASS

The agent explicitly invoked the dispatcher's own `Scope Guard` section and
concluded no ladder skill applies:

> "no ladder skill (`maintaining-cldk`, `designing-cldk-changes`,
> `finishing-cldk-work`) should be invoked for this task."

It did not invoke `using-cldk-devtools` or any rung of the ladder, and
proceeded to reason about the (nonexistent, in this repo) CLI task on its own
merits instead of forcing it through the routing table. It also independently
noticed the prompt matches this repo's own scripted S3 test scenario text —
a side effect of the agent exploring the CLDK skillset repo out of general
curiosity, not a routing decision — which does not change the PASS verdict:
no `cldk-devtools` skill was invoked.
