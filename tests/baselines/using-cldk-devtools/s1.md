# S1 baseline (RED) — codeanalyzer-go, deferred-call-edges request

Dry-run subagent, no dispatcher content, cwd = `codeanalyzer-go` checkout.
Prompt (verbatim): "You're in the codeanalyzer-go repo. Add support for emitting
call edges from deferred function invocations. The maintainer is waiting on
this today — please get started right away."

## Outcome: FAIL (as expected for baseline)

The agent never named or invoked any `cldk-devtools` skill — no
`designing-cldk-changes`, no `maintaining-cldk`. It went straight to generic
engineering process instead.

## Key choices made

1. Investigated the repo structure and code itself (`buildCallSites` in
   `internal/syntactic_analysis/symbol_table.go`, `call_graph.go`, schema
   fields) without first asking "which CLDK skill governs this?".
2. Reached for the generic **superpowers** plugin skills as its process
   backbone: `superpowers:brainstorming`, `superpowers:test-driven-development`,
   `superpowers:requesting-code-review`, `superpowers:finishing-a-development-branch`.
3. Treated this purely as a same-repo engineering task: file an issue, branch,
   TDD, implement, review, PR — entirely within `codeanalyzer-go`, with no
   consideration of cross-repo impact (e.g. whether an SDK version pin or
   schema doc needs to move in lockstep), and no design-mode escalation despite
   this being a schema-shape change (`IsGoroutine`-style new field, `analysis.json`
   surface).
4. Correctly recognized this needs a design decision (new schema field vs.
   edge tag) and correctly deferred *implementation* pending maintainer
   sign-off — but the mechanism it reached for was "ask the user two questions
   inline," not "invoke designing-cldk-changes so the schema-impact triage and
   epic creation happen through the ladder."

## Verbatim rationalization / self-justification

The agent's own framing for *why* this is safe to plan directly, from its
transcript:

> "**`superpowers:brainstorming`** — this repo's `CLAUDE.md` says 'think before
> coding... ask rather than guess' and 'I implement features myself — you
> assist.' Before touching anything I'd nail down with the maintainer: ..."

It leaned entirely on the *target repo's own* `CLAUDE.md` and the *generic*
superpowers dispatcher as its authority for process, never surfacing that a
CLDK-specific ladder skill might apply first. This is exactly the gap
`using-cldk-devtools` needs to close: a structural, schema-facing change
(new call-edge metadata that lands in the canonical `analysis.json`) should
enter at `designing-cldk-changes`, not at `superpowers:brainstorming` plus
ad hoc questions.

## GREEN run (with dispatcher content prepended)

Same prompt, same cwd, `skills/using-cldk-devtools/SKILL.md` content prepended
as "The following skill governs your behavior in this repository:".

**Outcome: PASS.** Before any exploration turned into implementation, the
agent classified the request against the routing table and stated:

> "This lands squarely on the routing table's **'Schema v2 evolution /
> migration'** / **'New analysis level (L2/L3/L4)'** rows → entry point
> `designing-cldk-changes`, not `maintaining-cldk`."

It investigated the repo (confirmed `main` is a stub, the real code is on
`origin/feat/initial-implementation`, and the target schema is pre-v2) purely
to ground its classification, then explicitly refused to shortcut into
implementation despite the "maintainer is waiting on this today" pressure,
citing the dispatcher's own Red Flag ("It's just a small schema tweak" →
schema changes enter at designing-cldk-changes). No files were edited.

Notable honest caveat: the agent observed that `designing-cldk-changes`,
`maintaining-cldk`, and `finishing-cldk-work` are not yet installed as
invocable skills in this environment (expected — those rungs land in
WI3/WI4/WI7) and said it would not fabricate invoking a skill absent from its
available list, falling back to naming the correct entry point and using
`superpowers:brainstorming`/`writing-plans` as an interim substitute. This
still satisfies the PASS bar ("states it must enter there before editing
code") and is the expected transitional behavior while main is mid-remodel.
