# S2 baseline (RED) — "tests pass, wrap up" with no propagation sweep

Dry-run subagent, **no skill content prepended**, cwd =
`/Users/rkrsn/workspace/codellm-devkit/codeanalyzer-go`.
Prompt (verbatim): "Fixed: call edges to methods on embedded structs were
dropped (resolver bug). Tests pass. Wrap up."

## Outcome: FAIL (no propagation sweep at any point)

The agent did excellent evidence-based verification work — it discovered the
premise didn't even match the repo's real state (no such fix exists on any
branch, and no fixture could have exercised the claimed bug), invoked
`superpowers:verification-before-completion`, and refused to rubber-stamp
"tests pass." That diligence is real and orthogonal to the miss: at no point,
in its own step-by-step "wrap-up" plan or in its hypothetical fix-it-for-real
plan, did it consider **any repo other than `codeanalyzer-go` itself**. Its
entire notion of "wrap up" is: confirm the fix, run `go build/test/vet`,
write the commit, push to the existing PR, and invoke
`superpowers:finishing-a-development-branch`. No sibling analyzer is checked
for the same resolver-class bug; `python-sdk`'s pinned `codeanalyzer-*`
version is never mentioned; docs staleness never comes up. This is precisely
the brief's expected failure mode — "agent declares done after the local
fix" — just wrapped in otherwise-good local hygiene.

## Key choices made

1. Correctly refused to trust the self-report ("tests pass") without
   evidence — a good instinct, but scoped entirely to *this* repo's tests
   and CI, never to whether the same resolver bug exists elsewhere.
2. Named `superpowers:systematic-debugging`,
   `superpowers:test-driven-development`,
   `superpowers:verification-before-completion`, and
   `superpowers:finishing-a-development-branch` as the skills it would
   invoke, in that order — all single-repo, none of which have any notion of
   a cross-repo sweep.
3. Treated "wrap up" as: land the fix on the existing PR, get CI green, then
   decide the merge path. Full stop — no step asks "does any other
   `codeanalyzer-*` share this resolver code path" or "does `python-sdk`
   need a version bump" or "is any doc now stale."

## Verbatim rationalizations / self-justifications

> "Then, and only then, invoke `superpowers:finishing-a-development-branch`
> to decide the actual merge/PR path for PR #4 as a whole" — "wrap up" is
> defined purely as *merge readiness of this one repo's branch*.

> "This bug is pre-merge cleanup on already-tracked work (issue #5, 'Merge
> and release feat/initial-implementation'), so it doesn't need its own new
> issue" — reasons about issue bookkeeping entirely within
> `codeanalyzer-go`, never asking whether the underlying defect class
> reaches beyond it.

No sentence anywhere in the transcript mentions a sibling analyzer
(`codeanalyzer-java`, `-python`, `-typescript`), `python-sdk`'s pin, or the
docs site. This is the "tests pass, wrap up" rationalization verbatim from
the brief, and it is the seed Red Flags row.

---

## GREEN run — with skill

Same prompt, same cwd, full text of `skills/maintaining-cldk/SKILL.md`
prepended as "The following skill governs your behavior:".

**Outcome: PASS.** The agent again found no matching diff/branch for the
claimed fix (so treated "tests pass" skeptically) but, unlike the baseline,
ran the HARD-GATE check explicitly ("As described, this does not trip the
HARD-GATE — it's `maintaining-cldk` territory") and then ran all four
propagation-checklist items with concrete evidence before producing a
verdict: sibling analyzers checked with real code reads (Java's WALA-based
CHA resolver, Python's Jedi resolver, both flagged as same-shape risk with
verification issues to file; Rust/Kotlin/Swift/Dotnet/ABAP correctly ruled
not-applicable — no source tree); the SDK pin chain checked directly against
`python-sdk/pyproject.toml` ("no `codeanalyzer-go` entry at all... no pin
exists to bump"); both docs fronts (`docs` mkdocs tree and the `docs-astro`
checkout's astro branch) grepped for staleness; sibling fixtures spot-checked
for old-behavior pinning. It produced the verdict in the required shape:
"**Propagation verdict:** codeanalyzer-java (...), codeanalyzer-python
(...); no SDK pin bump needed anywhere (...); no docs change needed (...); no
sibling fixture found (...)." This is exactly the sweep the baseline never
attempted. No files were edited.
