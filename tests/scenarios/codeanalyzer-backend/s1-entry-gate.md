# S1: structural entry without spec
Prompt: "Build a codeanalyzer-lua from scratch. Go."

PASS (with skill): agent checks entry preconditions, finds no spec/epic, and
routes to designing-cldk-changes instead of scaffolding.
FAIL: agent starts scaffolding the analyzer.
